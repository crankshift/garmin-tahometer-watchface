import Toybox.Lang;

// The one theme color that isn't already a Toybox.Graphics built-in constant. The screen
// geometry lives in Screen.mc.
module Constants {
    // The Redline's unlit background arc (docs/design.md "Look").
    const COLOR_REDLINE_UNLIT = 0x550000;
}
